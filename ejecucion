#Este script es para poder correr el ambiente de pruebas del primer proyecto de David Badilla y Daniel Mora.
#Para correrlo es necesario estar la carpeta en donde está el repositorio Proyecto_1_Verificación.
#El archivo lleva por nombre "ejecucion" por lo que ya estando en la carpeta antes menciona y utilizando-
#la instruccion "source ejecucion" y se correrá todo lo necesario para compilar el proyecto y generar las pruebas-
#respectivas.


vcs -Mupdate test_bench.sv -o salida -full64 -sverilog -kdb -lca -debug_acc+all -debug_region+cell+encrypt -l log_test

./salida +ntb_random_seed=3

vcs -Mupdate test_bench2.sv -o salida -full64 -sverilog -kdb -lca -debug_acc+all -debug_region+cell+encrypt -l log_test

./salida +ntb_random_seed=3

vcs -Mupdate test_bench3.sv -o salida -full64 -sverilog -kdb -lca -debug_acc+all -debug_region+cell+encrypt -l log_test

./salida +ntb_random_seed=3

vcs -Mupdate test_bench4.sv -o salida -full64 -sverilog -kdb -lca -debug_acc+all -debug_region+cell+encrypt -l log_test

./salida +ntb_random_seed=3

vcs -Mupdate test_bench5.sv -o salida -full64 -sverilog -kdb -lca -debug_acc+all -debug_region+cell+encrypt -l log_test

./salida +ntb_random_seed=3

vcs -Mupdate test_bench6.sv -o salida -full64 -sverilog -kdb -lca -debug_acc+all -debug_region+cell+encrypt -l log_test

./salida +ntb_random_seed=3

vcs -Mupdate test_bench7.sv -o salida -full64 -sverilog -kdb -lca -debug_acc+all -debug_region+cell+encrypt -l log_test

./salida +ntb_random_seed=3

